import {StyleSheet} from 'react-native'

const iconStyles = StyleSheet.create({
    tabsIconContainer: {
        width: 50,
        height: 30,
        borderRadius: 28,
        justifyContent: 'center',
        alignItems: 'center',
    }
})

export {iconStyles}